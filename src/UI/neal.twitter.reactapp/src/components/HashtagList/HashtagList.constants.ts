/** Represents string literals used in the HashtagList component. */
export enum HashtagListMessages {
    /** Label used for the Tweet count element. */
    COUNT_LABEL = 'Total Tweets that have been processed by this API:',
}